import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'school-organization-member-list',
  webDir: 'dist'
};

export default config;
